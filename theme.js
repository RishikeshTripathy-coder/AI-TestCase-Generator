import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      //   main: "#4A4AFF",
      main: "#3E3A9E",
    },
    text: {
      primary: "#333333",
    },
  },
});

export default theme;
